name: webview_youtube_bug
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  youtube_player_iframe: ^2.1.0 # this lib is not being used but it is in here to mimetize tenants-app behavior cause it has flutter_inappwebview as a dependency
  flutter_inappwebview: ^5.3.2
  flutter_webview_plugin: 
    git:
      url: git://github.com/micaelcid/flutter_webview_plugin.git
      ref: fix/webview-state-changed

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true